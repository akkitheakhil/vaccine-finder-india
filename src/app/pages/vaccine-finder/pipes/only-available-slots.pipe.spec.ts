import { OnlyAvailableSlotsPipe } from './only-available-slots.pipe';

describe('OnlyAvailableSlotsPipe', () => {
  it('create an instance', () => {
    const pipe = new OnlyAvailableSlotsPipe();
    expect(pipe).toBeTruthy();
  });
});
