import { FilteredSlotCountPipe } from './filtered-slot-count.pipe';

describe('FilteredSlotCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteredSlotCountPipe();
    expect(pipe).toBeTruthy();
  });
});
